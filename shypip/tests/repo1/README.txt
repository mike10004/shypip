# Local repository for testing

Start an HTTP server in this directory with this command:

    $ python3 -m http.server 8080

Then use `pip install --extra-index-url http://localhost:8080 <package>`.
